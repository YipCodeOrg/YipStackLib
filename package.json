{
    "name": "yipstacklib",
    "private": true,
    "devDependencies": {
        "@types/jest": "^28.1.6",
        "jest": "^28.1.3",
        "ts-jest": "^28.0.7"
    },
    "scripts": {
        "init-ts-jest": "ts-jest config:init",
        "test": "jest"
    }
}
